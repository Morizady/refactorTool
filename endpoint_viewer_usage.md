# Java项目接口查看功能使用指南

## 功能介绍

`--show-endpoint` 参数允许你快速查看Java Spring Boot项目中特定接口的详细信息，包括：
- 接口基本信息
- 完整的调用链分析
- 相关文件列表
- SQL映射信息
- 源代码片段

## 使用方法

### 基本语法
```bash
python main.py --show-endpoint "接口路径"
```

### 使用示例

#### 1. 查看分页查询接口
```bash
python main.py --show-endpoint "/admin/category/page"
```

输出示例：
```
================================================================================
🔍 接口详细信息
================================================================================
📋 基本信息:
  接口名称: CategoryController.page
  请求路径: GET /admin/category/page
  控制器: CategoryController
  处理方法: page
  源文件: test_projects\sky-take-out\JavaWeb\sky-take-out\sky-server\src\main\java\com\sky\controller\admin\CategoryController.java
  行号: 46
  复杂度: 37
  框架: spring

🔗 调用链分析:
  方法调用 (3个):
     1. log.info() - 2个参数 (行:49)
     2. categoryService.pageQuery() - 1个参数 (行:50)
     3. Result.success() - 1个参数 (行:51)

📁 相关文件 (7个):
  Service层:
    - CategoryService.java
    - CategoryServiceImpl.java
  DTO对象:
    - CategoryPageQueryDTO.java
    - DishPageQueryDTO.java
    - EmployeePageQueryDTO.java

📝 源代码片段:
  文件: CategoryController.java
  方法: page (行 48-52)
  ------------------------------------------------------------
       48:     public Result<PageResult> page(CategoryPageQueryDTO categoryPageQueryDTO){
       49:         log.info("分页查询：{}", categoryPageQueryDTO);
       50:         PageResult pageResult = categoryService.pageQuery(categoryPageQueryDTO);
       51:         return Result.success(pageResult);
       52:     }
```

#### 2. 查看登录接口
```bash
python main.py --show-endpoint "/admin/employee/login"
```

#### 3. 模糊匹配查看
```bash
python main.py --show-endpoint "login"
```

如果有多个匹配的接口，系统会列出所有选项供你选择：
```
🔍 找到 2 个匹配的接口:
1. POST /admin/employee/login - EmployeeController.login
2. POST /user/user/login - UserController.login

请选择要查看的接口 (输入序号): 1
```

## 功能特点

### 1. 智能匹配
- 支持完整路径匹配：`/admin/category/page`
- 支持部分路径匹配：`category/page`
- 支持关键词匹配：`login`

### 2. 详细信息展示
- **基本信息**: 接口名称、路径、控制器、方法名等
- **调用链**: 显示方法调用的顺序和参数数量
- **相关文件**: 按类型分组显示Service、DTO、VO等文件
- **SQL映射**: 显示相关的数据库操作
- **源代码**: 自动定位并显示方法的完整代码

### 3. 源代码高亮
- 自动定位方法定义
- 高亮显示接口定义行
- 显示完整的方法体代码

### 4. 复杂度评估
- 显示接口的复杂度得分
- 帮助识别需要重构的复杂接口

## 前置条件

使用此功能前，需要先运行单项目分析生成分析数据：

```bash
# 先分析项目生成数据
python main.py --single test_projects/sky-take-out

# 然后查看特定接口
python main.py --show-endpoint "/admin/category/page"
```

## 错误处理

### 1. 分析文件不存在
```
❌ 分析文件不存在: ./migration_output/endpoint_analysis.json
请先运行单项目分析生成分析数据：
python main.py --single /path/to/project
```

### 2. 接口未找到
```
❌ 未找到匹配的接口: /api/test

可用的接口路径:
  - GET /admin/category/page
  - POST /admin/category/status/{status}
  - GET /admin/category/list
  ... 还有 28 个接口
```

### 3. 源文件不存在
```
📝 源代码片段:
  ❌ 源文件不存在: /path/to/file.java
```

## 实用技巧

### 1. 快速查看所有接口
如果输入一个不存在的接口路径，系统会显示前10个可用接口作为参考：
```bash
python main.py --show-endpoint "nonexistent"
```

### 2. 查看复杂接口
通过复杂度得分识别需要关注的接口：
- 复杂度 > 40: 高复杂度，需要重点关注
- 复杂度 20-40: 中等复杂度
- 复杂度 < 20: 简单接口

### 3. 分析调用链
通过调用链分析了解接口的执行流程：
- 方法调用数量反映接口复杂度
- 调用顺序帮助理解业务逻辑
- 参数数量提示数据传递情况

### 4. 查看相关文件
通过相关文件列表快速定位：
- Service层：业务逻辑实现
- DTO对象：数据传输对象
- VO对象：视图对象
- Mapper层：数据访问层

## 与其他功能的配合使用

### 1. 配合单项目分析
```bash
# 先分析项目
python main.py --single /path/to/project --verbose

# 查看分析报告
cat migration_output/analysis_report.md

# 查看特定接口
python main.py --show-endpoint "/admin/category/page"
```

### 2. 配合详细分析工具
```bash
# 使用详细分析工具
python detailed_code_analyzer.py

# 查看特定接口
python main.py --show-endpoint "/admin/employee/login"
```

## 总结

`--show-endpoint` 功能为开发者提供了一个快速查看和理解接口实现的工具，特别适用于：

- 🔍 **代码审查**: 快速了解接口实现逻辑
- 📚 **学习研究**: 学习优秀项目的代码结构
- 🐛 **问题排查**: 定位接口相关的问题
- 📖 **文档生成**: 为接口生成详细文档
- 🔧 **重构准备**: 识别需要重构的复杂接口

通过这个功能，你可以更高效地理解和维护项目代码！